import React from"react";import{Plus,Search,Filter,MoreHorizontal}from"lucide-react";export function EntityPage({title,description,rows,columns}:{title:string;description:string;rows:any[];columns:string[]}){return <div><div className="heading"><div><h1>{title}</h1><p>{description}</p></div><button className="primary"><Plus size={15}/>Add {title.replace(/s$/,'').toLowerCase()}</button></div><div className="panel"><div className="toolbar"><div className="searchbox"><Search size={15}/><input placeholder={`Search ${title.toLowerCase()}...`}/></div><button><Filter size={15}/>Filters</button></div><div className="table"><table><thead><tr>{columns.map(c=><th>{c}</th>)}<th/></tr></thead><tbody>{rows.slice(0,18).map((r:any)=><tr>{Object.values(r).slice(0,columns.length).map((v:any,j)=><td>{j===columns.length-1?<span className="status">{String(v)}</span>:String(v)}</td>)}<td><MoreHorizontal size={16}/></td></tr>)}</tbody></table></div></div></div>}
